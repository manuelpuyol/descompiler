types {
  int as inteiro;
}

functions {
  int sum(int a, int b);
}

data {
  int globalVar; `this is a comment
}

int sum(int a, int b) {
  data {
    int c;
  }

  c = a + b;
  return c;
}

int main() {
  data {
    int result;
  }

  result = sum(a, b);
  println(result)

  return 0;
}
`this is a comment on EOF