types {
  int as inteiro;
  char as letra;
}

functions {
  int mul(int a, int b);
}

data {
  int global_var;
}

int mul(int a, int b) {
  data {
    int c;
  }

  c = a * b;
  return c;
}

int main() {
  data {
    int result;
  }

  result = mul(a, b);
  println(result)

  return 0;
}