types {
  int as inteiro;
  char as letra;
}

functions {
  int sum(int a, int b);
  int mul(int a, int b);
}

data {
  int global_var; `this is a comment
}

int sum(int a, int b) {
  data {
    int c;
  }

  c = a + b;
  return c;
}

int mul(int a, int b) {
  data {
    int c;
  }

  c = a * b;
  return c;
}

int main() {
  data {
    int result;
  }

  result = sum(a, b);
  println(result)

  return 0;
}
`this is a comment on EOF